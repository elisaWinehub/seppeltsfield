mutation {
  blogCreate(blog: { title: "Journal", handle: "journal" }) {
    blog {
      id
      title
      handle
      templateSuffix
    }
    userErrors {
      field
      message
    }
  }
}
