query JournalArticle($id: ID!) {
  article(id: $id) {
    id
    title
    handle
    tags
    publishedAt
    author {
      name
    }
    image {
      id
      url
      altText
    }
    body
  }
}
