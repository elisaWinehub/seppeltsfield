mutation UpdateJournalArticle($id: ID!, $article: ArticleUpdateInput!) {
  articleUpdate(id: $id, article: $article) {
    article {
      id
      title
      handle
      publishedAt
      tags
      image {
        id
        url
        altText
      }
    }
    userErrors {
      field
      message
    }
  }
}
